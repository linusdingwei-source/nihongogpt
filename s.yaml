edition: 3.0.0
name: nihongogpt-project
access: default

resources:
  nihongogpt_app:
    component: fc3
    props:
      region: cn-hongkong
      functionName: nihongogpt-web
      description: 'Next.js standalone app on FC 3.0'

      runtime: custom-container
      customContainerConfig:
        image: docker.io/nihongogpt/nihongogpt:latest
        port: 3000

      # 建议配置：增加内存以避免 OOM 或冷启动过慢
      cpu: 1.0
      memory: 2048
      timeout: 60

      # 必须开启公网访问，否则无法连接 Stripe 或 数据库
      internetAccess: true

      # 环境变量映射
      environmentVariables:
        NODE_ENV: production
        # 这里读取的是上面 GitHub Workflow 中 'env:' 下定义的变量
        DATABASE_URL: ${env.DATABASE_URL}
        AUTH_SECRET: ${env.AUTH_SECRET}
        AUTH_TRUST_HOST: "true"
        NEXTAUTH_URL: ${env.NEXTAUTH_URL}
        GOOGLE_CLIENT_ID: ${env.GOOGLE_CLIENT_ID}
        GOOGLE_CLIENT_SECRET: ${env.GOOGLE_CLIENT_SECRET}
        STRIPE_SECRET_KEY: ${env.STRIPE_SECRET_KEY}
        STRIPE_WEBHOOK_SECRET: ${env.STRIPE_WEBHOOK_SECRET}

      triggers:
        - triggerName: httpTrigger
          triggerType: http
          triggerConfig:
            authType: anonymous
            methods: [GET, POST, PUT, DELETE, OPTIONS]